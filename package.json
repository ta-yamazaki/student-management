{
  "name": "firebase",
  "description": "Firebase",
  "scripts": {
    "nodeAndLogin": "npm --prefix ./functions install ./functions --save && firebase login --no-localhost",
    "emulate": "firebase emulators:start --import=./.saved-data --export-on-exit"
  }
}
